package seleniumCommands;

import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class CheckBox {

	public static void main(String[] args) {
		//testing check box

		WebDriver driver = new ChromeDriver();
		driver.get("http://www.training.qaonlinetraining.com/testPage.php");
		//List <WebElement> CheckBoxs = driver.findElements(By.cssSelector("input [type="CheckBox"]"));
	// helps to clear all the checked boxes form before
		// use for loop
		//for(WebElement Check: CheckBoxs) {
		//	CheckBox.click();
		//}
		//if(checkBox is selected()) {
		//	checkBox.click();
	//}
		//if(CheckBox.getAttribute("value").contains("bick")) 
		//CheckBox.click();
		//{
			
		
	
		
	}

}
